# This repo uses two .env files — copy the examples below into each app folder.

# --- server/.env ---
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"
# CLIENT_ORIGIN=
# PORT=4000

# --- client/.env ---
# VITE_API_URL=http://localhost:4000
